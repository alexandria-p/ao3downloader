- a button at the top of the page - one to DOWNLOAD NEW BOOKMARKS, one to UPDATE EXISTING DOWNLOADED WORKS
- eventually, combine these into a single button press
- eventually, host this somewhere simple (github pages?)

upload to, and read from, dropbox link (instead of local downloads folder)
-checkbox for which types to download
- HTML and JSON are REQUIRED (for the site code to run), all others are optional.

how to handle cancel midway through download (especially for html with images?)
- this didnt really work when tested.

can we improve 'update incomplete fics' -> to instead search to see if json has changed (eg update date or chapter count) since last pull. I think existing system is outdated and is redownloading ALL incomplete works, but will not catch if complete works add bonus chapters, like jane dutch.


is data.json needed in /build/config?



long term:

filtering in-app
make html look nice, load images into html fics.



for github pages - do not create downloads folder automatically. all buttons are greyed out until a downloads folder is selected  (make this a deployment option, that updates settings.ini, that is read by project)

for github pages - do not bother keeping logs (make this a deployment option, that updates settings.ini, that is read by project)

ask claude to help with how to configure this for github pages, and that for every push to /main should redeploy the new build.

stop trawl once you catch up to where you were (optional, starts as ticked).

'update bookmarks marked as incomplete' - eh still buggy, can we do better?


allow to add a collection by url instead only being able to index your own
New buttons:
(index my collections) - the existing button in the app
(index collection by URL)



if a collection Work or collection Bookmark does not match any indexed works in your downloads folder, just display it as the work ID & link to the work on ao3 for that entry in the table.

data.json is still being built in build/config when i run the app. i told you not to build it anymore since we dont need it.

also - show the settings used for current crawl in the modal (e.g. from the settings.ini file being references)

add a option for start page, not just stop page, on Download newly added bookmarks

is there a way to make downloading fics more efficient, so you only need to crawl a page once (instead of for every type)? For example, can we download PDF, HTML and EPUB with a single page hit?

If so, then here is how downloads should work:

- JSON crawl first
- then, per-fic,
- download all checked types for that fic with a single hit.

