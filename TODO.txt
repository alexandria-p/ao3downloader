upload to, and read from, dropbox link (instead of local downloads folder)
-checkbox for which types to download
- HTML and JSON are REQUIRED (for the site code to run), all others are optional.

Doco:
- import existing from another location (modal & ID)
- what each button in the app does
- what if you import undated? show what happens during download/update modal
- 'skipped 2 bookmarks that are not works (series, external works, or deleted works)' -> ensures an exportable list is available at end, with the reason for each
- what is indexing
- EnableDebugTools=true in settings.ini


long term:

filtering in-app
make html look nice, load images into html fics.



for github pages - do not create downloads folder automatically. all buttons are greyed out until a downloads folder is selected  (make this a deployment option, that updates settings.ini, that is read by project)

for github pages - do not bother keeping logs (make this a deployment option, that updates settings.ini, that is read by project)

ask claude to help with how to configure this for github pages, and that for every push to /main should redeploy the new build.

data.json is still being built in build/config when i run the app. i told you not to build it anymore since we dont need it.

option to backdate individual (undated) files - instead of only in bulk?


update readme 
- if you make dev changes to website, you can just hot-reload.
- if you make dev changes to downloader process, you will need to restart the worker (by exiting uv.exe and rerunning start-application.ps1


Cosmetic improvements:


-----------

starting
indexing: saving a json file for every bookmark before downloading any works
finished page 1 of 80. 20 works so far
finished page 2 of 80. 40 works so far
skipped 2 bookmarks that are not works (series, external works, or deleted works)
generating list of work links that are already in the downloads folder (will be skipped)
checking which of these you have already downloaded
checking which of your downloads ao3 has a newer version of
9 downloaded works have been updated on ao3 since you saved them
downloading works
downloading 58 works straight from the index, without walking the listing again
stopping at your request...
stopped at your request. anything already saved has been kept.


is this because 9 existing need updating, and 49 are new and arent downloaded at all??
let's fix the output wording here. cause it is confusing AF



what happens when stopped or exited mid-download?

-- -Index writes are now per page, as you asked. get_metadata buffers a page's records and saves them only once the whole page is parsed — previously it saved after each blurb, which made a part-read page a part-written index by definition. --> should i change this?????








Get works from all encountered series links
If a bookmarked work belongs to a series, download the whole series too. This can take a great deal longer. Series you bookmarked directly are always downloaded either way. why cant i choose this for all download types?

how to download a bookmarked series?

/// bleh think this is already completed
when you do not know for sure how many pages need indexing examples include in the quickscan (and in custom scan) - for example if we are indexing up until a date, indexing between dates, indexing only new bookmarks etc....then don't show 'fetching page X out of Y' in the console or progress bar caption -> because we dont actually know what page Y IS (ie when we will stop).
why was data.json still built in build/config?? we do not need this file, remove anything that builds in to build/config.

be able to pause a work IN progress, not AFTER work in progress


what happens when i pause during login? It says "pausing after the work in progress" and 'appears to have paused' - what is actually happening functionally?



Custom option & full index 
- do it in date order (of existing indexes, go from most recently updated first)
- instead of page-page, have an option to re-index (and redownload, if needed) if existing index is within a last-updated date range.


add debug options in gui, if turned on in settings.ini
- skip to next workflow step
- skip to step that checks for deleted/failed works, and mockup a result with each type

when updating incomplete fics - do it in date order (of existing indexes, go from most recently updated first)


in 'Download new bookmarks and update incomplete fics' AND just update any bookmarks marked as incomplete AND download/update a specific fic AND full scan reindex & update all AND custom run

when Re-reading each unfinished fic, make the debug message for each fic more descriptive -->
you have no copy of this one, downloading it


(e.g. 
you have no copy in format XYZ - downloading now
you have an outdated version in format XYZ - replacing now
you already have the current version in format XYZ 
)


clarify in readme --> Download new bookmarks and update incomplete fics, in the third pass ( any finished fic missing a format you asked for) - update the index for any fic that is missing a format you asked for, then download/update those formats (as it does currently). -->  that we do this because the second pass indexes & downloads incomplete works, saving us time - the third pass in theory should only be remaining works missing a format and all these works are completed.

also please ensure that custom run and full scan both will download any missing formats for all fics.


for every single workflow (button) add a new collapsible panel to the download modal page (similar to 'settings this run is using'. call it 'steps in this run' or something similar. it should list out all expected steps in the workflow, and have a tick next to it once its complete, some indicator that it is in progress - maybe a spinning wheel, and an indicator if it has not started yet - maybe a timer (and a cross if it failed). this is so users can see what it left to do in the current workflow.

-- One caveat: AO3 logins do not last forever. A run left paused for a long time may find its session has expired when you resume, which shows up as works failing to download. If that happens, stop the run and start a new one. -> detect & handle this gracefully.


