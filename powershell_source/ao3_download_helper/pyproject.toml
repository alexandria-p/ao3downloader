[project]
name = "ao3downloader"
version = "2026.7.3"
description = "Utility for downloading fanfiction in bulk from the Archive of Our Own"
readme = "README.md"
license = "GPL-3.0-only"
license-files = ["LICENSE"]
requires-python = ">=3.10,<3.13"
authors = [
    { name = "nianeyna", email = "nianeyna@gmail.com" }
]
keywords = [
    "ao3",
    "archive of our own",
    "fanfiction",
    "downloader",
    "bulk download",
    "ao3 downloader",
]
dependencies = [
    "beautifulsoup4>=4.13.4",
    "mobi>=0.3.3",
    "pypdf>=6.14.2",
    "requests>=2.32.5",
    "tqdm>=4.67.1",
]

[project.scripts]
ao3downloader = "source_code:main.ao3downloader"

[project.urls]
Homepage = "https://nianeyna.dev/ao3downloader"
Repository = "https://github.com/nianeyna/ao3downloader"

# the distribution is "ao3downloader" but the package folder is source_code,
# so hatchling has to be told where to look
[tool.hatch.build.targets.wheel]
packages = ["source_code"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "pytest-cov>=6.2.1",
    "syrupy>=4.9.1",
]

[options.package-data]
source_code.html = ["*.html"]
source_code.settings = ["*.ini"]

[[tool.uv.index]]
name = "testpypi"
url = "https://test.pypi.org/simple/"
publish-url = "https://test.pypi.org/legacy/"
explicit = true

[tool.pytest.ini_options]
markers = [
    "ebook: exercises a downloaded ebook fixture file (auto-applied in conftest.py)",
]
